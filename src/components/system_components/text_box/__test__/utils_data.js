export const fieldmeta = {
  name: "textbox",
  title: "enter value",
  type: "TEXTBOX",
  placeHolder: "select textbox",
  info: "select value",
  visible: false,
  canUpdate: false,
  disable: false,
  required: true,
  validationRegEx:
    /[\w]+((?:[\w-]+\.)*[A-Za-z0-9])+@(?:[A-Za-z0-9-]+\.)+[\w]{2,}/,
  visibleOnCsv: false,
};

export const data = {};
